// ignore_for_file: file_names

import 'package:flutter/material.dart';

class FinishShoppingWidget extends StatelessWidget {
  const FinishShoppingWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Finish Shopping"),
      ),
    );
  }
}
