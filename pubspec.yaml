name: laba
description: A new project to learn how to use ARCore in your Flutter App

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts:
  ar_flutter_plugin: ^0.6.4
  google_nav_bar: ^5.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/triangle.png
    - assets/Chicken_01/
    - assets/images/
    - assets/locker/
